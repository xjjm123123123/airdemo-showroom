# Gemini API 配置示例
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_BASE_URL=https://api.zhizengzeng.com/v1/

# 飞书多维表格 Webhook 配置
# 
# 开发环境：
#   - 使用 Vite 代理（vite.config.ts 中配置）
#   - 无需配置此变量
#
# 生产环境（Vercel）：
#   - 使用 Serverless 函数代理（api/webhook.js）
#   - 使用相对路径，自动适配域名（推荐）
#
VITE_WRITE_VIOLATION_WEBHOOK=/api/webhook

# ============================================
# 飞书 Aily（首页智能助手）接入配置
# ============================================
#
# 说明：前端首页对话建议调用你自己的 /api/aily 代理接口，避免在浏览器暴露密钥。
# - 生产环境：直接用相对路径 /api/aily（走 Vercel Serverless）
# - 本地开发：如果你不跑 vercel dev，可以先把它指到线上域名，例如：
#   VITE_AILY_CHAT_ENDPOINT=https://<你的域名>/api/aily
#
VITE_AILY_CHAT_ENDPOINT=/api/aily

# ============================================
# Vercel 环境变量（在 Vercel 控制台配置）
# ============================================
# 
# 以下变量需要在 Vercel 项目设置中配置，不要提交到 Git
#
# 1. 前端环境变量（会注入到浏览器）
#    - GEMINI_API_KEY=sk-zk234bab01427c84ba5f4a0f1f1fa633e336943dce1a4495
#    - GEMINI_BASE_URL=https://api.zhizengzeng.com/v1/
#    - VITE_WRITE_VIOLATION_WEBHOOK=https://airdemo.cn/api/webhook
#
# 2. 后端环境变量（仅 Serverless 函数可访问）
#    - FEISHU_WEBHOOK_URL=https://bytedance.larkoffice.com/base/automation/webhook/event/Oo1MaNfsZwEpMMhIMGBcQEIqnYb
#    - LARK_APP_ID=cli_xxx
#    - LARK_APP_SECRET=xxx
#    - AILY_APP_ID=xxxx  （Aily 应用 ID，来自 Aily 页面地址栏参数）
#
# 配置步骤：
#   1. 登录 Vercel Dashboard
#   2. 进入项目 → Settings → Environment Variables
#   3. 添加上述变量
#   4. 选择 Production, Preview, Development（全选）
#   5. 保存并重新部署
